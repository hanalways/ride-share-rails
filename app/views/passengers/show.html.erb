<section class="header">
  <ul>
    <li>
      <img src="http://placekitten.com/150/150">
    </li>
    <li>
      <%= @passenger.name %>
    </li>
    <li>
      <%= @passenger.phone_num %>
    </li>
  </ul>
  <button type="button">Request Ride<button>
</section>

<h2>Trips</h2>
<%# Trip.where(passenger_id: passenger_id) %>

<aside>
  <ul>
    <li>
      <%= link_to "Edit", edit_passenger_path(@passenger) %>
    </li>
    <li>
      <%= link_to "Delete", passenger_path(@passenger), method: :delete, data: { confirm: "Do you wish to delete #{@passenger.name}?" } %>
    </li>
  </ul>
</aside>